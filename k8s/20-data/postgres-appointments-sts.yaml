apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-appointments
  namespace: medplatform-data
spec:
  serviceName: postgres-appointments
  replicas: 1
  selector:
    matchLabels:
      app: postgres-appointments
  template:
    metadata:
      labels:
        app: postgres-appointments
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: password
        - name: POSTGRES_DB
          value: appointments_db
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "postgres", "-d", "appointments_db"]
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-appointments
  namespace: medplatform-data
spec:
  clusterIP: None
  selector:
    app: postgres-appointments
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
